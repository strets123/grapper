.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install grapper

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv grapper
    $ pip install grapper
